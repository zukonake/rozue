#pragma once

/**
 * Represents data sent from the Client to the Server.
 *
 * ClientData will contain input data for the Server
 * to parse, the Server will forward that data to the
 * Player through Connection. For example the data is:
 * what key was clicked, where the mouse is, etc.
 */
struct ClientData
{

};
