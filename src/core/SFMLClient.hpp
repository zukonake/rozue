#pragma once

/**
 * SFMLClient is the IO interface for Client.
 *
 * SFMLClient manages the input: mouse and keyboard
 * and also the output: screen and sound. It accepts
 * orders from its corresponding Client.
 */
class SFMLClient
{
public:
private:
};
